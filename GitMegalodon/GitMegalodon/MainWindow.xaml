<Window x:Class="GitMegalodon.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GitMegalodon"
        mc:Ignorable="d"
        Title="GitMegalodon" Height="800" Width="1200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- Left Panel - Repository List and Actions -->
        <DockPanel Grid.Column="0" Background="#2D2D30">
            <TextBlock DockPanel.Dock="Top" Text="GitMegalodon" Foreground="White" 
                       FontSize="20" Padding="10" HorizontalAlignment="Center"/>
            <Button DockPanel.Dock="Top" Content="Open Repository" Margin="10,5" Padding="5"/>
            <Button DockPanel.Dock="Top" Content="Clone Repository" Margin="10,5" Padding="5"/>
            <Button DockPanel.Dock="Top" Content="Create Repository" Margin="10,5" Padding="5"/>
            
            <TextBlock DockPanel.Dock="Top" Text="Recent Repositories" Foreground="White" 
                       Padding="10" Margin="0,10,0,0"/>
            <ListBox Background="Transparent" Foreground="White" BorderThickness="0">
                <!-- Placeholder for repository list -->
                <ListBoxItem>Repository 1</ListBoxItem>
                <ListBoxItem>Repository 2</ListBoxItem>
                <ListBoxItem>Repository 3</ListBoxItem>
            </ListBox>
        </DockPanel>
        
        <!-- Right Panel - Repository Details -->
        <TabControl Grid.Column="1" Background="#1E1E1E">
            <TabItem Header="History">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Branch & Action Toolbar -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#333337">
                        <ComboBox Width="150" Margin="0,0,10,0">
                            <ComboBoxItem IsSelected="True">main</ComboBoxItem>
                            <ComboBoxItem>develop</ComboBoxItem>
                        </ComboBox>
                        <Button Content="Pull" Margin="5,0"/>
                        <Button Content="Push" Margin="5,0"/>
                        <Button Content="Fetch" Margin="5,0"/>
                    </StackPanel>
                    
                    <!-- Commit Graph Area -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Commit Graph -->
                        <ListView Grid.Column="0" Background="Transparent">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Graph" Width="120"/>
                                    <GridViewColumn Header="Description" Width="300"/>
                                    <GridViewColumn Header="Author" Width="150"/>
                                    <GridViewColumn Header="Date" Width="150"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                        
                        <!-- Commit Details -->
                        <DockPanel Grid.Column="1" Background="#252526">
                            <TextBlock DockPanel.Dock="Top" Text="Commit Details" Foreground="White" 
                                      FontSize="16" Padding="10" Background="#333337"/>
                            <StackPanel>
                                <TextBlock Text="Title: Initial commit" Foreground="White" Margin="0,5"/>
                                <TextBlock Text="Author: User Name" Foreground="White" Margin="0,5"/>
                                <TextBlock Text="Date: 2023-08-21 14:30" Foreground="White" Margin="0,5"/>
                                <TextBlock Text="Files Changed:" Foreground="White" Margin="0,15,0,5"/>
                                <ListView Height="200" Background="#1E1E1E" BorderThickness="1" BorderBrush="#333337">
                                    <!-- File changes list -->
                                </ListView>
                                <TextBlock Text="Commit Message:" Foreground="White" Margin="0,15,0,5"/>
                                <TextBox Height="100" IsReadOnly="True" Background="#1E1E1E" 
                                        Foreground="White" BorderThickness="1" BorderBrush="#333337"/>
                            </StackPanel>
                        </DockPanel>
                    </Grid>
                </Grid>
            </TabItem>
            
            <TabItem Header="Changes">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Stage/Unstage Actions -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#333337">
                        <Button Content="Stage All" Margin="5,0"/>
                        <Button Content="Unstage All" Margin="5,0"/>
                        <Button Content="Refresh" Margin="5,0"/>
                    </StackPanel>
                    
                    <!-- Changes Area -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Files List -->
                        <DockPanel Grid.Column="0" Background="#252526">
                            <!-- First section with Staged Changes -->
                            <ListView Background="Transparent" Foreground="White" BorderThickness="0">
                                <!-- Placeholder for staged files -->
                            </ListView>
                            
                            <TextBlock DockPanel.Dock="Top" Text="Unstaged Changes" Foreground="White" 
                                      FontSize="14" Padding="10" Background="#333337"/>
                            <ListView Background="Transparent" Foreground="White" BorderThickness="0">
                                <!-- Placeholder for unstaged files -->
                            </ListView>
                        </DockPanel>
                        
                        <!-- Diff View -->
                        <DockPanel Grid.Column="1" Background="#1E1E1E">
                            <TextBlock DockPanel.Dock="Top" Text="Diff View" Foreground="White" 
                                      FontSize="14" Padding="10" Background="#333337"/>
                            <TextBox IsReadOnly="True" Background="#1E1E1E" Foreground="White" 
                                    BorderThickness="0" FontFamily="Consolas" Padding="10"/>
                        </DockPanel>
                    </Grid>
                    
                    <!-- Commit Actions -->
                    <DockPanel Grid.Row="2" Background="#252526">
                        <Button DockPanel.Dock="Right" Content="Commit" Padding="10,5" Margin="10,0,0,0"/>
                        <TextBox DockPanel.Dock="Top" Height="80" Margin="0,0,0,10" 
                                Text="Commit message..." TextWrapping="Wrap"/>
                        <TextBox Text="Author: User &lt;user@example.com&gt;" Height="25"/>
                    </DockPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>